<%==
  { :page      => @breweries.current_page,
    :pages     => @breweries.total_pages,
    :total     => @breweries.total_entries,
    :breweries => @breweries.collect { |brewery|
      { :id         => brewery.id,
        :name       => brewery.name,
        :created_at => brewery.created_at,
        :updated_at => brewery.updated_at
      }
    }
  }.to_json
%>
