<%==
  { :page    => @brewers.current_page,
    :pages   => @brewers.total_pages,
    :total   => @brewers.total_entries,
    :brewers => @brewers.collect { |brewer|
      { :id   => brewer.id,
        :name => brewer.name
      }
    }
  }.to_json
%>
